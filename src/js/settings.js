export const Settings = {
    //General settings
    screenWidth: window.innerWidth * (window.innerWidth/1920),
    screenHeight: window.innerHeight * (window.innerHeight/1080),
    screensizeFactorWidth: window.innerWidth/1920,
    screensizeFactorHeight: window.innerHeight/1080,
    gravityStrength: 3000,

    //Player settings
    playerY: 660,
    playerX: 400,

    //Enemy settings
    enemySpeed: 600,
}